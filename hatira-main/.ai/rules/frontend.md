# Frontend copy

- User-facing React strings are written in Turkish in the TSX files. There is no frontend i18n layer.
- Always use "parola" for password, never "şifre".
- Use "isimsiz" for unnamed impressions, never "anonim".
